const fetch = require("./index")
const start = new fetch({logs: true})
start.post({
    /*
    url: "endpoint url",
    auth: "authorization or type none",
    body: "data to post"
    */
})
